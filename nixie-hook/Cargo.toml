[package]
name = "nixie-hook"
version = "1.0.0"
edition = "2021"
description = "Cursor Hooks handler for Nixie desktop pet — maps agent events to pet mood"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
